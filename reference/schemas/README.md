# reference/schemas — grammars pulled from recursive.eco-schemas (read-only reference)

**Why this folder exists (Jul 3 2026):** so a claude.ai / Claude Code session working *inside the
kali-paradevi repo* can read these directly — without cross-referencing another repo (the trip
environment blocks reaching sibling projects). **Reference copies**, not kali-paradevi's own
grammars (those live in `grammars/`). Source of truth:
[`PlayfulProcess/recursive.eco-schemas`](https://github.com/PlayfulProcess/recursive.eco-schemas).

Selected because they touch the sky / deep-sky adjacency (constellations are the naked-eye map over
the objects Kali·Paradevi names). Note: schemas has no galaxy/black-hole grammar today — the deep-sky
canon lives in this repo's own `grammars/kali.json` + `grammars/paradevi.json`.

| Grammar | What it is |
|---|---|
| `embodied-grammars-constellation` | a constellation grammar (14 items) |
| `kids-constellation` | constellation stories/storyboards/media (68 items) |

**License:** CC-BY-SA-4.0 (via each file's `_grammar_commons`); attribution travels with the file.
